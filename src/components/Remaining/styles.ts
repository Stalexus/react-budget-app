import styled from "styled-components";
import { StyledBadge } from '../Badget/styles';

export const StyledCardRemaining = styled(StyledBadge)`
    justify-content: flex-start;
    background-color: #CCD5FF;
`